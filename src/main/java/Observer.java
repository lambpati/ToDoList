/**
 * Meant to help define what an Observer is
 * and be implemented to define how Observers should be notified.
 */
public interface Observer {
    /**Notify Observers following change */
    void notifyObservers();
}
